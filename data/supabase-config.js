/* ============================================================
   Supabase 연결 설정 — 회원가입 · 로그인 · 텐들이 직접 업로드
   ------------------------------------------------------------
   아래 두 값을 채우기 전까지는 로그인 기능이 자동으로 꺼진 상태로
   동작해요(에러 없이 "준비 중" 문구만 보여요). 사이트의 다른 기능은
   이 파일과 무관하게 그대로 작동해요.

   값을 얻는 방법과 필요한 SQL 스크립트는 SUPABASE_SETUP.md 를 보세요.
   ============================================================ */
window.TENTRIOR_SUPABASE = {
  url: 'https://gsjuqynppsbjnbvdnnja.supabase.co',
  anonKey: 'sb_publishable_5RptKHF707LyUjc8DBcWtg_ROQJXcIH'
};

/* 주의
   - anonKey 는 "공개해도 되는" 키예요. 브라우저 코드에 그대로 넣어도 안전하도록
     설계돼 있어요 (실제 데이터 접근 권한은 Supabase의 RLS 정책이 통제해요).
   - service_role 키(관리자 키)는 어떤 경우에도 이 파일이나 사이트 코드에
     넣지 마세요. 그건 Supabase 대시보드에서만 사용하는 값이에요. */
